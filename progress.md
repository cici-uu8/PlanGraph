# Progress: Plangraph Standalone Transition

## 2026-06-18

- Created task tracking files for the multi-phase repository transition and implementation.
- Inspected current README, zh README, root SKILL, plugin metadata, OpenAI metadata, wrapper skill, templates, and `scripts/plan_governance.py`.
- Rewrote README, README.zh-CN, root SKILL, plugin metadata, OpenAI metadata, and wrapper skill into `PlanGraph` / `$plangraph` public language while retaining current script path as implementation detail.
- Added in-memory `PlanGraph` query core in `scripts/plan_governance.py` with `graph mainline`, `graph lineage`, and `graph impact`.
- Added graph integrity lint checks for supersession cycles and orphan parents.
- Added `unittest` coverage for registry parsing, graph queries, integrity errors, and CLI JSON output.
- Regenerated README assets so the hero image uses PlanGraph messaging.
- Verification passed: `python3 -m py_compile scripts/plan_governance.py scripts/generate_readme_assets.py`, `python3 -m unittest discover -s tests -p 'test_*.py'`, CLI smoke for graph JSON queries, and bootstrap smoke that writes `.plangraph.yml` / `.plangraph.ignore` without PyYAML installed.
- Continued `v0.2.1` stabilization: config writes now always use the internal YAML dumper, `graph mainline` reports `derivation` and explanatory notes, and governance command regression tests cover `register`, `close`, `supersede`, and lint.
- Started `v0.3` deterministic enhancement: added `graph conflicts` for registry-derived hard conflicts and wired lint to reuse the same conflict checks.
- Completed `v0.3` integrity/conflict boundary cleanup: structural graph errors stay in `integrity_errors`, while parent lifecycle contradictions are reported only through `graph conflicts`.
- Continued `v0.3` dependency cleanup: config and frontmatter reads now fall back to the internal YAML parser when PyYAML is unavailable or fails, with regression tests for both paths.
- Added `examples/github-actions/plangraph-lint.yml` as a copyable CI lint template for governed user repositories.
- Continued `v0.3` Phase 4: added read-only Markdown body-link extraction via `graph body-links [plan_id]`, including relative path resolution, heading anchor checks, `body-link` provenance, and unresolved reference reporting. This does not write inferred links back to the registry.
- Wired unresolved body-link references into `lint` so broken registered-document links are visible during normal governance checks.
- Ran Phase 4 Stop/Go on `/Users/cici/oncall agent/super_biz_agent_py-plan-update`: `edge_count=1`, `unresolved_count=8`, `unresolved_ratio=88.89%`, all unresolved links were `outside-repo`. Decision: Stop SQLite; continue with lightweight external-reference classification only.
- Added structured `external_reference` classification for outside-repo local Markdown links. Real validation now reports `edge_count=1`, `external_reference_count=8`, `external_existing_count=8`, `unresolved_count=0`; adding `/Users/cici/oncall agent/super_biz_agent_py-release-2026-03-21` as an in-memory trusted root marks all 8 external refs trusted. Decision remains Stop SQLite because repo-local edges are sparse and external refs are context, not stable current-repo graph edges.
- Added `adopt-external-references` for external Markdown localization: dry-run by default; `--apply` copies useful outside-repo Markdown references into `external_reference_import_dir`, rewrites source links to repo-relative links, registers imported docs as non-authoritative governed context, and refreshes the timeline.
- Completed a real oncall-repo external-reference adoption pass: after merging legacy ignore rules into `.plangraph.ignore`, the remaining 22 historical plan / closeout / evidence docs were registered as non-current governed documents, leaving the timeline with 5 executable mainline heads and no conflicts. Final verification on the real repo was `lint: ok`, `edge_count=9`, `unresolved_count=0`, `external_reference_count=4`, and `graph conflicts=0`.
- Release-state decision for the deterministic line: freeze `v0.3.x` at `v0.3.2`. Do not tag `v0.4.0` for external-reference adoption; reserve `v0.4` for SQLite once a new Go condition is proven in real usage.
- User decision changed the execution path: continue local-only development directly into SQLite / MCP / semantic layer without waiting for a second external-repo validation pass; do not push to GitHub until the broader roadmap reaches a complete boundary.
- Started `v0.4 SQLite`: added `index` and `status` CLI commands. `index` rebuilds `.plangraph/plangraph.db` from registry rows, registry edges, body-link edges, unresolved refs, external refs, and tracked file fingerprints. `status` is read-only and reports schema/count/staleness JSON.
- Added default `.plangraph/**` scan exclusion so generated SQLite/cache files do not enter plan discovery, even if a project includes `.plangraph/**/*.md` in scan globs.
- Added SQLite regression tests for index creation, stale registry detection, missing legacy config tracking, and `.plangraph` scan exclusion.
- Verification passed after SQLite first slice: `python3 -m py_compile scripts/plan_governance.py scripts/generate_readme_assets.py tests/test_governance_commands.py tests/test_plan_graph.py`; `python3 -m unittest discover -s tests -p 'test_*.py'` reports 30 tests OK.
- Real oncall plan-update smoke for SQLite first slice: `index` produced `.plangraph/plangraph.db` with `node_count=54`, `edge_count=69`, `file_count=57`, `unresolved_count=0`, `external_reference_count=4`, and `stale=false`.
- Continued `v0.4 SQLite`: added schema version 2 with `node_fts`, `sync` for stale or old-schema rebuilds, and `query <text>` for SQLite-backed plan search. `query` refuses missing/stale indexes and tells the user to run `index` or `sync` instead of silently reading old data.
- Verification passed after SQLite query/sync slice: `python3 -m py_compile scripts/plan_governance.py tests/test_governance_commands.py`; `python3 -m unittest discover -s tests -p 'test_*.py'` reports 34 tests OK.
- Real oncall plan-update smoke for SQLite query/sync slice: `sync` reports `action=noop` after schema 2 rebuild; `query RAG` returns 20 FTS results from the indexed registry/body corpus with `stale=false`.
- Started `v0.5 MCP`: added a minimal stdio JSON-RPC MCP server through the `mcp` command. It supports `initialize`, `tools/list`, and `tools/call` for `plangraph_status`, `plangraph_mainline`, and `plangraph_query`, reusing the existing SQLite/graph functions instead of creating a second implementation.
- Verification passed for MCP first slice: targeted MCP test OK, full `python3 -m unittest discover -s tests -p 'test_*.py'` reports 35 tests OK, and a real oncall plan-update stdio smoke returned status with `node_count=54`, `edge_count=69`, `schema_version=2`, `stale=false`.
- Started `v0.6 semantic layer`: added schema version 3 with `semantic_edges` and an explicit `semantic` command. The first slice uses deterministic token overlap only; edges are `semantic-inferred` soft hints with confidence/shared terms, never registry facts and never fatal lint.
- Verification passed for semantic first slice: full `python3 -m unittest discover -s tests -p 'test_*.py'` reports 36 tests OK. Real oncall plan-update smoke generated `semantic_edge_count=42`, `schema_version=3`, and `stale=false`.
- Recorded the local thaw rationale in `decisions/2026-06-18-thaw-v0.3-freeze.md`: the author deliberately resumed local SQLite/MCP/semantic product-foundation work after the `v0.3.x` freeze, not because an external reviewer requested it.
- Continued SQLite/MCP/semantic stabilization: schema version 4 adds `relation_scope` to semantic edges; ordinary `query` no longer returns `semantic_results`; explicit `semantic` output now only prioritizes high-confidence `registry-zero-relation` soft edges outside the same workstream.
- Expanded the MCP stdio tool surface to include `plangraph_lineage`, `plangraph_impact`, `plangraph_conflicts`, and `plangraph_body_links` in addition to status, mainline, and query. These tools reuse the existing graph engine and remain read-only.
- After external review, reclassified the supported release surface: deterministic registry/graph/SQLite/MCP remains the public boundary, while `semantic` stays a local experimental command. The real validation repo reduced 42 raw overlap candidates to 1 incrementally useful edge, which is enough to keep the command but not enough to justify more feature investment right now.
- Dogfooded PlanGraph on `/Users/cici/anomaly_detection` as an external-user trial. Accepted two deterministic-core P0 fixes: adoption reports now distinguish repo Markdown count, configured scan-scope count, skipped in-scope files, and out-of-scope uninspected files; `strict_mainline` now reports multiple active execution heads in one workstream even when `authoritative=false`, and lint fails until the user pins, closes, or supersedes down to one head.
- Added deterministic `context` v1 for `plan_id` on both CLI and MCP: `graph context <plan_id>` and `plangraph_context` now aggregate mainline, lineage, impact, plan-specific conflicts, explicit body links, and a deduplicated `must_read` list without introducing semantic soft edges. Verification passed with `python3 -m py_compile ...`, `python3 -m unittest discover -s tests -p 'test_*.py'` reporting 42 tests OK, and `git diff --check`.
